1) make a dictionary of jobs from the initial request, and for each jobextendeddetails response, add its required documents to the dictionary
2) start implementing the waiting for user to click on everything logic

im gonna make a chrome extension. when the user turns it on, a button pops out at the top of the page that says "start/stop reading". the user will click on the button, and then the extension will start waiting to read in handshake job data. the user will have to manually click on every single job on the handshake page, and once the user's done with that page, the user clicks the "stop reading" button. a list of all the job data is sent to a serverless endpoint on aws lambda, where a langchain pipeline of stuff like parsing the job data, calling chatgpt to analyze each of the keywords in the data, calling chatgpt to decide which of the user's experiences are most relevant for the job, calling chatgpt to generate a tailored section for each of the user's experiences for the job, and then putting everything together into an organized resume, will run on the job data sent to it. the tailored resume will be returned to the extension in pdf form. after that, the extension automatically creates a spreadsheet of all the jobs teh user clicked on, and generates a tailored resume based on the user's resume for each of the jobs, and puts the link to the tailored resume pdf in the spreadsheet. after this, the user can manually go to each of the jobs and input the tailored resume pdf themselves.